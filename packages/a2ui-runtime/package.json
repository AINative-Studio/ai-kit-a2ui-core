{
  "name": "@ainative/a2ui-runtime",
  "version": "0.1.0",
  "description": "A2UI Runtime adapters for LLM providers",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "build": "tsup"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.20.0",
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^1.0.0",
    "openai": "^4.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "peerDependencies": {
    "@anthropic-ai/sdk": "^0.20.0",
    "openai": "^4.0.0"
  }
}
